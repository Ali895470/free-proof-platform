'use client';
import ProtectedRoute from '@/lib/ProtectedRoute';

export default function DashboardPage() {
  return (
      <ProtectedRoute>
            <div>
                    <h1>لوحة التحكم</h1>
                            {/* يمكنك عرض إحصائيات أو بيانات هنا */}
                                  </div>
                                      </ProtectedRoute>
                                        );
                                        }